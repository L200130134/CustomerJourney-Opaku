<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.rikyahmadfathoni.test.opaku">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Opaku"
        android:usesCleartextTraffic="true"
        tools:targetApi="m">
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.ProductActivity"
            android:theme="@style/Theme.Opaku.NoActionBar" />
        <activity
            android:name=".activity.CartActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".activity.AddAddressActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".activity.InputActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".activity.OrderDetailsActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".activity.ChangePasswordActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".activity.PersonalInfoActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".activity.BankOrderActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".activity.OrderListActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".activity.AddressActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".activity.BankSelectorActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".activity.CheckoutActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".activity.CardSelectorActivity"
            android:launchMode="singleTask" />
        <activity android:name=".activity.ImagePreviewActivity"
            android:launchMode="singleTask" />
    </application>

</manifest>